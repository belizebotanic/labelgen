{
  "name": "labelgen",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Browser-based visual SVG label-sheet editor",
  "license": "MIT",
  "scripts": {
    "dev": "web-dev-server --config web-dev-server.config.mjs",
    "test": "web-dev-server --config web-dev-server.config.mjs --open /tests/"
  },
  "devDependencies": {
    "@open-wc/dev-server-hmr": "^0.2.0",
    "@web/dev-server": "^0.4.6"
  }
}
