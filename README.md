# labelgen (web)

Browser-based visual editor for SVG label sheets — design, preview with CSV data,
and export. No backend; no production build toolchain.

Setup, dev server, deploy, and architecture notes are added in later commits.
